"""Inferencia del modelo de detección de daños.

Carga el modelo PyTorch entrenado (modelo_danos.pt) y lo expone como
una función que recibe un par de imágenes y devuelve el nivel de daño
y la confianza para cada edificio del mapa base.

TODO (Adriana): cargar_modelo(), inferir(imagen_pre, imagen_post, poligonos).
"""
